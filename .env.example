# SQLite en local (fichier). Pour la prod, utiliser une vraie base
# (ex: postgresql://user:pass@host:5432/dbname) et changer le provider
# dans prisma/schema.prisma en "postgresql".
DATABASE_URL="file:./dev.db"

# Générer avec: openssl rand -base64 32
NEXTAUTH_SECRET="ksd22042001"
NEXTAUTH_URL="http://localhost:3000"
